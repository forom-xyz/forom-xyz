{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind", "config", "plugin", "source", "theme", "utility", "variant", "custom-variant", "apply"]
      }
    ]
  }
}
